<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:l="using:AvClientView.MarkupExtensions"
             xmlns:vmc="using:AvClientMvvmContract.Multiplayer.CnCNet"
             x:Class="AvClientView.Multiplayer.CnCNet.LoadOrSaveGameOptionPresetWindow"
             x:DataType="vmc:ILoadOrSaveGameOptionPresetWindowViewModel"
             IsVisible="{Binding IsVisible}">
  <Border Background="{DynamicResource XnaPanelBackgroundBrush}"
          BorderBrush="{DynamicResource XnaPanelBorderBrush}"
          BorderThickness="1"
          Padding="16"
          Width="350"
          HorizontalAlignment="Center"
          VerticalAlignment="Center">
    <StackPanel Spacing="10">
      <!-- Preset list -->
      <ListBox ItemsSource="{Binding PresetNames}"
               SelectedIndex="{Binding SelectedPresetIndex}"
               Height="200"
               Background="#80000000" />

      <!-- New preset name (for save mode) -->
      <StackPanel Spacing="4" IsVisible="{Binding IsNewPresetNameEnabled}">
        <TextBlock Text="{l:Translate 'Preset Name:', 'Client:UI:PresetName'}" Foreground="{DynamicResource XnaSubtleTextBrush}" />
        <TextBox Text="{Binding PresetName}" />
      </StackPanel>

      <!-- Buttons -->
      <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
        <Button Content="{l:Translate 'Confirm', 'Client:UI:ButtonConfirm'}"
                Command="{Binding ConfirmCommand}"
                Width="92" Height="23"
                IsEnabled="{Binding IsConfirmEnabled}" />
        <Button Content="{l:Translate 'Delete', 'Client:UI:ButtonDelete'}"
                Command="{Binding DeleteSelectedPresetCommand}"
                Width="92" Height="23"
                IsEnabled="{Binding IsDeleteEnabled}" />
        <Button Content="{l:Translate 'Cancel', 'Client:UI:ButtonCancel'}"
                Command="{Binding CancelCommand}"
                Width="92" Height="23" />
      </StackPanel>
    </StackPanel>
  </Border>
</UserControl>
